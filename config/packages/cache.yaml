framework:
    cache:
        prefix_seed: flyovercms

        # Dev: filesystem. Prod: Redis (see when@prod below)
        app: cache.adapter.filesystem

when@prod:
    framework:
        cache:
            app: cache.adapter.redis
            default_redis_provider: '%env(REDIS_URL)%'
